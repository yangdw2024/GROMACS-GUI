# GROMACS GUI v4.2 开发说明文档

## 一、项目概述

本项目为Windows平台自研GROMACS动力学模拟集成GUI工具，基于Python + PyQt5开发，面向分子动力学模拟科研用户。

---

## 二、本次重构内容清单

### 2.1 新增文件

| 文件路径 | 功能说明 |
|---------|---------|
| `source/core/version_manager.py` | 版本管理器：扫描、去重、过滤、PLUMED校验 |
| `source/core/crash_handler.py` | 崩溃捕获框架：全局异常捕获、弹窗、导出 |
| `version.config` | 全局版本配置文件 |
| `build_release.bat` | 标准化打包脚本 |

### 2.2 修改文件

| 文件路径 | 修改内容 |
|---------|---------|
| `source/core/__init__.py` | 导出新模块 |
| `source/core/gromacs_service.py` | 添加兼容方法 |
| `source/core/logger.py` | 修复构造函数参数 |
| `source/core/audit_mechanism.py` | 添加兼容方法 |
| `source/core/resource_monitor.py` | 添加GPU驱动版本 |
| `source/core/auto_updater.py` | 添加字典访问支持 |
| `source/gromacs_gui_v3.py` | 集成VersionManager、CrashHandler、版本切换交互 |

---

## 三、版本管理模块 (version_manager.py)

### 3.1 设计目标

解决原版本列表冗余、无效版本未过滤、PLUMED校验粗糙的问题。

### 3.2 核心类

**VersionMetadata**
- 存储单个版本的完整元数据
- 包含：名称、路径、版本号、SIMD、GPU、PLUMED状态、有效性状态、哈希ID、构建参数

**VersionManager**
- `scan_versions()` - 扫描版本，支持过滤和去重
- `_validate_version()` - 校验版本有效性（gmx.exe、力场、CUDA、PLUMED）
- `_validate_plumed_detailed()` - PLUMED精细化校验
- `_deduplicate_versions()` - 基于文件哈希去重
- `select_best_version()` - 选择最佳版本
- `delete_version()` - 删除版本目录

### 3.3 PLUMED校验逻辑

```
1. 检查版本是否声称支持PLUMED
   - gmx --version 输出中包含 "plumed support: yes"
   - 或版本名称中包含 "plumed"

2. 检查PLUMED文件是否存在
   - libplumedKernel.dll
   - libplumed.dll
   - Plumed.h

3. 如果文件缺失 -> PlumedStatus.MISSING_FILES
4. 如果文件存在但为空/不可读 -> PlumedStatus.BROKEN_LIBRARY
5. 如果全部正常 -> PlumedStatus.SUPPORTED
6. 如果未声明支持 -> PlumedStatus.NOT_SUPPORTED
```

### 3.4 去重逻辑

```
1. 计算版本目录的哈希ID
   - 基于关键文件(gmx.exe, libgromacs.dll)的MD5
   - 加上目录大小和修改时间

2. 比较哈希ID
   - 如果两个版本的哈希ID相同 -> 标记为重复
   - 保留第一个发现的版本为主版本
```

---

## 四、崩溃捕获模块 (crash_handler.py)

### 4.1 设计目标

解决原程序崩溃仅落地日志、无前端提示的问题。

### 4.2 核心类

**CrashReport**
- 存储崩溃报告数据：时间、异常类型、消息、堆栈、线程、系统信息
- 支持导出为文本和JSON格式

**CrashHandler**
- `install()` - 安装全局异常捕获（替换sys.excepthook）
- `_handle_exception()` - 处理异常，生成报告，保存日志，弹出提示
- `export_crash_log()` - 导出崩溃日志
- `get_crash_logs()` - 获取崩溃日志列表

### 4.3 前端集成

在GromacsGUI.__init__中：
```python
self.crash_handler = CrashHandler()
self.crash_handler.install()
self.crash_handler.add_listener(self._on_crash_report)
```

`_on_crash_report()` 方法弹出QMessageBox提示框，包含：
- 异常类型和消息
- 崩溃ID和时间
- 详细堆栈信息
- "导出崩溃日志"按钮

---

## 五、打包构建系统

### 5.1 打包脚本 (build_release.bat)

**流程：**
1. 读取version.config获取版本号
2. 前置校验：Python、PyInstaller、源码文件
3. 清理旧构建文件
4. 创建version_info.json
5. PyInstaller编译（--onefile --windowed）
6. 整理产物到release/目录
7. 创建zip压缩包

**输出：**
```
release/
  Trae_Gromacs_GUI_v4.2.0.exe
  启动程序.bat
  version.config
  config/
  resources/
```

### 5.2 版本配置 (version.config)

```json
{
  "product_name": "Trae_Gromacs_GUI",
  "version_string": "4.2.0",
  "major_version": 4,
  "minor_version": 2,
  "patch_version": 0
}
```

修改此文件后运行build_release.bat即可生成新版本的exe。

---

## 六、版本更新规范

### 6.1 小版本更新（bug修复）

1. 修改patch_version + 1
2. 修改源码中的bug
3. 运行build_release.bat重新打包
4. 替换旧版本exe

### 6.2 大版本更新（功能新增）

1. 修改minor_version + 1，patch_version = 0
2. 开发新功能模块
3. 更新说明书.md
4. 运行build_release.bat重新打包
5. 全量替换发布

---

## 七、故障排查指南

### 7.1 版本扫描为空

- 检查gromacs/目录是否存在
- 检查版本目录下是否有bin/gmx.exe
- 查看日志中的扫描结果

### 7.2 版本切换失败

- 查看弹窗提示的具体错误
- 检查gmx.exe是否可执行
- 检查CUDA/PLUMED依赖是否完整

### 7.3 打包失败

- 检查build.log中的错误信息
- 确认Python和PyInstaller已安装
- 确认源码文件完整

### 7.4 崩溃日志分析

崩溃日志位于：`source/logs/crashes/crash_YYYYMMDD_HHMMSS.log`

包含信息：
- 异常类型和消息
- 完整调用堆栈
- Python版本和操作系统
- 程序启动参数

---

## 八、开发环境

### 8.1 依赖安装

```cmd
pip install PyQt5 numpy psutil pyinstaller
```

### 8.2 目录结构规范

```
source/core/       - 核心框架模块
source/gui/        - GUI组件
gromacs/           - GROMACS版本目录
config/            - 配置文件
logs/              - 日志文件（运行时生成）
release/           - 打包输出（构建时生成）
```

### 8.3 代码规范

- 使用中文注释
- 类名使用PascalCase
- 方法名使用snake_case
- 常量使用UPPER_CASE
- 异常处理使用try/except并记录日志

---

**文档版本**: v4.2.0  
**更新日期**: 2026-07-09