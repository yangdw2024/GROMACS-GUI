# GROMACS GUI v4.2 使用说明书

## 目录

1. [软件概述](#1-软件概述)
2. [系统要求](#2-系统要求)
3. [文件结构](#3-文件结构)
4. [版本更新说明](#4-版本更新说明)
5. [GROMACS版本说明](#5-gromacs版本说明)
6. [核心功能模块](#6-核心功能模块)
7. [使用方法](#7-使用方法)
8. [版本管理](#8-版本管理)
9. [崩溃处理](#9-崩溃处理)
10. [打包发布](#10-打包发布)
11. [故障排除](#11-故障排除)
12. [附录](#12-附录)

---

## 1. 软件概述

GROMACS GUI是一款基于Python和PyQt5开发的分子动力学模拟平台，集成了多个版本的GROMACS，提供可视化的模拟配置和管理界面。

**主要特点：**
- 支持多版本GROMACS自动扫描和管理
- 自动检测CPU/GPU硬件信息
- 智能版本选择（基于性能评分）
- 版本自动去重和无效版本过滤
- PLUMED精细化校验（三种场景区分）
- 全局崩溃捕获+前端弹窗提示
- 一键导出崩溃日志
- 完整的模拟工作流管理
- 实时资源监控和日志记录
- 自动纠错和审计机制

---

## 2. 系统要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10/11 64位 | Windows 11 64位 |
| CPU | Intel i5 / AMD Ryzen 5 | Intel i9 / AMD Ryzen 9 |
| 内存 | 8 GB | 16 GB+ |
| GPU | NVIDIA GPU (可选) | NVIDIA RTX 5060 Ti |
| CUDA | 11.x+ | 13.x |
| Python | 3.10+ | 3.13 |

---

## 3. 文件结构

```
D:\YDW\Trae_Gromacs\
├── source/                    # 源代码目录
│   ├── gromacs_gui_v3.py      # GUI主程序入口
│   ├── core/                  # 核心框架模块
│   │   ├── config_manager.py     # 配置管理
│   │   ├── logger.py             # 日志系统
│   │   ├── error_handler.py      # 错误处理
│   │   ├── resource_monitor.py   # 资源监控
│   │   ├── event_bus.py          # 事件总线
│   │   ├── review_mechanism.py   # 审查机制
│   │   ├── correction_mechanism.py # 纠错机制
│   │   ├── audit_mechanism.py    # 审计机制
│   │   ├── auto_updater.py       # 自动更新
│   │   ├── version_manager.py    # 版本管理器(新增)
│   │   ├── crash_handler.py      # 崩溃捕获(新增)
│   │   └── gromacs_service.py    # GROMACS服务
│   └── gui/                   # GUI组件
├── gromacs/                   # GROMACS版本目录
├── release/                   # 打包输出目录(新增)
├── config/                    # 配置文件
├── logs/                      # 日志文件
├── version.config             # 全局版本配置(新增)
├── build_release.bat          # 标准化打包脚本(新增)
└── 说明书.md                  # 本文档
```

---

## 4. 版本更新说明

### v4.2.0 (2026-07-09) - 全量重构版本

**模块1：打包构建系统重构**
- 新增标准化打包脚本 `build_release.bat`
- 修复原打包脚本乱码、路径解析错误问题
- 支持中文路径、空格路径、长路径
- 打包前置校验：Python环境、PyInstaller、源码完整性
- 自动输出带版本号的 `.exe` 可执行文件
- 打包产物统一归档至 `release/` 目录

**模块2：GUI稳定性&崩溃容错改造**
- 新增全局崩溃捕获框架 `crash_handler.py`
- 程序崩溃时弹出前端可视化提示框
- 展示精简崩溃原因、崩溃ID、时间
- 提供一键导出崩溃日志按钮
- 崩溃日志自动保存到 `logs/crashes/` 目录

**模块3：GROMACS版本管理系统重构**
- 新增版本管理器 `version_manager.py`
- 版本自动去重：识别同源重复编译产物
- 无效版本自动过滤：编译失败、文件缺失、损坏版本
- PLUMED精细化校验，区分四种场景：
  - `not_supported` - 不支持PLUMED
  - `missing_files` - PLUMED文件缺失
  - `broken_library` - PLUMED库损坏
  - `supported` - PLUMED支持完整
- 版本元数据存储：编译参数、有效性标签、哈希ID
- GUI内置版本清理工具

**模块4：全局版本更新机制标准化**
- 新增 `version.config` 全局版本配置文件
- 统一管控程序主版本号、次版本号、迭代号
- 所有源码、打包脚本、输出文件名读取该配置

**模块5：配套工程辅助能力优化**
- 版本切换交互优化：加载提示、状态栏信息、校验弹窗
- 路径兼容性全量适配
- 全模块代码注释和工程文档

---

## 5. GROMACS版本说明

### 5.1 版本列表（自动去重过滤后）

| 版本名称 | 版本号 | SIMD | GPU | PLUMED | 默认版本 |
|---------|-------|------|-----|--------|---------|
| gromacs-2026.3-AVX512-CUDA-sm120-final2 | 2026.3 | AVX-512 | CUDA | 不支持 | ✅ |
| gromacs-2026.3-AVX512-CUDA-PLUMED-sm120 | 2026.3 | AVX-512 | CUDA | 支持 | |
| gromacs-2026.3-AVX512-CUDA | 2026.3 | AVX-512 | CUDA | 不支持 | |
| gromacs-2026.3-AVX512-CUDA-PLUMED | 2026.3 | AVX-512 | CUDA | 支持 | |
| gromacs-2026.3-AVX2-CUDA | 2026.3 | AVX2 | CUDA | 不支持 | |
| gromacs-2026.1-plumed-CUDA | 2026.1 | AVX2 | CUDA | 文件缺失 | |
| gromacs-2025.1-SM120-AVX512 | 2025.1 | AVX-512 | CUDA | 不支持 | |

### 5.2 版本选择建议

| 场景 | 推荐版本 |
|------|---------|
| 常规GPU模拟（默认） | gromacs-2026.3-AVX512-CUDA-sm120-final2 |
| PLUMED模拟 | gromacs-2026.3-AVX512-CUDA-PLUMED-sm120 |
| 高性能计算 | gromacs-2026.3-AVX512-CUDA |
| CPU-only | gromacs-2026.3-AVX2-CUDA |

### 5.3 默认版本设置

默认启动时自动选择 `gromacs-2026.3-AVX512-CUDA-sm120-final2` 版本，该版本支持CUDA sm_120架构，兼容最新NVIDIA GPU。

```
D:\YDW\Trae_Gromacs\gromacs\<版本名称>\bin\gmx.exe
```

---

## 6. 核心功能模块

### 6.1 配置管理 (ConfigManager)
自动管理用户配置，支持配置持久化到JSON文件。

### 6.2 日志系统 (AppLogger)
支持六级日志：DEBUG、INFO、SUCCESS、WARNING、ERROR、CRITICAL

### 6.3 资源监控 (ResourceMonitor)
实时监控系统资源：CPU使用率、内存、GPU、磁盘

### 6.4 版本管理器 (VersionManager) - 新增
- 自动扫描GROMACS版本
- 版本去重：基于文件哈希识别同源编译产物
- 无效版本过滤：校验gmx.exe、力场文件、CUDA库
- PLUMED精细化校验
- 版本元数据存储和查询
- 版本清理工具

### 6.6 片段索引生成器 (Fragment Index Generator) - 新增
- 支持自定义片段名称和原子范围定义
- 批量生成多个片段组
- 自动扩展到多分子体系
- 生成标准GROMACS索引文件格式

### 6.7 片段-片段RDF分析 (Fragment-Fragment RDF) - 新增
- 支持任意两个片段之间的径向分布函数计算
- 批量处理多个片段对
- 支持分子质心RDF
- 自动生成xvg输出文件

### 6.8 轨迹预处理流水线 (Trajectory Preprocessing Pipeline) - 新增
- PBC修复（mol/res/cluster模式）
- 体系居中
- 旋转+平移拟合去除平动/转动
- 截取平衡段
- 降采样处理
- 一键完成全套预处理

### 6.9 能量分解分析 (Energy Decomposition) - 新增
- 从edr文件提取能量项
- 分析组分间相互作用能量
- 支持多种能量类型选择
- 自动生成能量变化曲线

### 6.10 自动绘图功能 (Auto Plot) - 新增
- 读取xvg文件生成科研图表
- 支持折线图、柱状图、散点图
- 多曲线对比图
- 符合期刊标准的图表样式

### 6.11 批量处理系统 (Batch Processing) - 新增
- 同时处理多个模拟体系
- 自动执行全套分析流程
- 统一管理批量任务
- 并行处理提升效率

### 6.12 RMSD/RMSF分析 - 新增
- RMSD: 计算体系/特定组的均方根偏差，评估结构稳定性
- RMSF: 计算残基级别的均方根涨落，分析结构柔性
- 支持自定义拟合组和计算组
- 自动生成xvg输出文件

### 6.13 MSD与扩散系数分析 - 新增
- 计算均方位移(MSD)随时间变化
- 线性拟合获取扩散系数
- 支持-trestart参数避免时间步冲突
- 支持指定分析组

### 6.14 SASA溶剂可及表面积分析 - 新增
- 计算体系或特定组的溶剂可及表面积
- 支持输出总面积和残基级SASA
- 分析结构折叠和暴露情况

### 6.15 氢键分析 - 新增
- 计算氢键数目随时间变化
- 支持供体-受体组指定
- 输出氢键存在率和平均寿命

### 6.16 回转半径分析 - 新增
- 计算体系或分子的回转半径
- 评估结构紧凑度和形状变化
- 输出主回转半径分量

### 6.17 团簇分析 - 新增
- GROMOS方法基于原子对距离进行结构聚类
- 输出团簇矩阵(.xpm)和大小分布
- 支持自定义cutoff距离
- -dista模式避免拟合要求

### 6.18 最近邻距离分析 - 新增
- 计算两个组之间的最近邻距离随时间变化
- 支持min/max/contact等多种距离类型
- 分析分子间接近程度

## 7. 使用方法

### 7.1 启动程序

**命令行启动：**
```cmd
cd D:\YDW\Trae_Gromacs
python source/gromacs_gui_v4.py
```

**双击启动：**
运行 `启动程序.bat` 或直接运行打包后的 `.exe` 文件

### 7.2 基本工作流

1. **选择版本**：在顶部版本选择下拉菜单中选择合适的GROMACS版本（默认sm120版本）
2. **配置模拟**：填写输入文件路径（gro、top、mdp）
3. **参数设置**：配置线程数、内存限制、GPU启用/关闭等参数
4. **运行模拟**：点击"开始模拟"按钮
5. **监控进度**：实时查看模拟进度和资源使用
6. **分析结果**：使用轨迹预处理、RDF分析、能量分解等工具
7. **生成图表**：使用自动绘图功能生成科研图表

### 7.3 后处理分析工作流

1. **片段定义**：使用片段索引生成器定义分析所需的片段
2. **轨迹预处理**：运行预处理流水线完成PBC修复、拟合、截取
3. **结构分析**：RMSD/RMSF/回转半径分析，评估结构稳定性和柔性
4. **相互作用分析**：RDF分析、氢键分析、SASA分析、最近邻距离
5. **动力学分析**：MSD分析，计算扩散系数
6. **结构分类**：团簇分析，识别主要构象簇
7. **能量分析**：提取并分析能量数据
8. **可视化**：生成图表展示分析结果

### 7.4 命令行使用

```cmd
# 使用指定版本
D:\YDW\Trae_Gromacs\gromacs\gromacs-2026.3-AVX512-CUDA-sm120-final2\bin\gmx.exe grompp -f md.mdp -c conf.gro -p top.top

# 运行模拟
D:\YDW\Trae_Gromacs\gromacs\gromacs-2026.3-AVX512-CUDA-sm120-final2\bin\gmx.exe mdrun -deffnm md -nt 8 -gpu_id 0
```

---

## 8. 版本管理

### 8.1 版本扫描

启动时自动扫描 `gromacs/` 目录下的所有版本：
- 自动去重：识别同源重复编译产物
- 自动过滤：剔除编译失败、文件缺失、损坏的版本
- PLUMED校验：区分支持/不支持/文件缺失/库损坏

### 8.2 版本切换

从下拉菜单选择版本时：
- 显示加载提示
- 自动校验版本有效性
- 状态栏显示当前版本、SIMD、GPU、PLUMED状态
- 校验失败弹出弹窗提示

### 8.3 版本清理

通过"版本信息"按钮打开版本管理对话框：
- 查看所有版本的状态（有效/重复/无效）
- 一键清理重复版本
- 一键清理无效版本
- 确认后不可撤销

---

## 9. 崩溃处理

### 9.1 崩溃捕获

程序崩溃时：
1. 自动捕获未处理异常
2. 生成崩溃报告（包含异常类型、消息、堆栈、系统信息）
3. 保存到 `logs/crashes/crash_YYYYMMDD_HHMMSS.log`
4. 弹出前端提示框，显示崩溃原因
5. 提供"导出崩溃日志"按钮

### 9.2 崩溃日志查看

崩溃日志位于：
```
D:\YDW\Trae_Gromacs\source\logs\crashes\crash_*.log
```

### 9.3 手动导出

崩溃提示框中点击"导出崩溃日志"，选择保存位置即可。

---

## 10. 打包发布

### 10.1 打包脚本

使用新的标准化打包脚本：
```cmd
D:\YDW\Trae_Gromacs\build_release.bat
```

### 10.2 打包流程

1. **前置校验**：检查Python、PyInstaller、源码文件
2. **清理旧文件**：删除旧的build/dist/release目录
3. **创建版本信息**：从 `version.config` 读取版本号
4. **编译打包**：使用PyInstaller编译为.exe
5. **整理产物**：复制配置文件、资源文件到release目录
6. **创建压缩包**：生成带版本号的zip文件

### 10.3 打包输出

```
release/
  +-- Trae_Gromacs_GUI_v4.2.0.exe
  +-- 启动程序.bat
  +-- version.config
  +-- config/
  +-- resources/

Trae_Gromacs_GUI_v4.2.0_Release.zip
```

### 10.4 版本配置

修改 `version.config` 更新版本号：
```json
{
  "version_string": "4.2.1",
  "major_version": 4,
  "minor_version": 2,
  "patch_version": 1
}
```

---

## 11. 故障排除

### 11.1 常见错误

| 错误信息 | 原因 | 解决方案 |
|---------|------|---------|
| 版本切换失败：版本无效 | gmx.exe不可执行 | 重新编译该版本 |
| PLUMED文件缺失 | libplumedKernel.dll不存在 | 重新编译带PLUMED的版本 |
| CUDA运行时库缺失 | cudart64_13.dll不存在 | 复制CUDA DLL到bin目录 |
| 程序崩溃 | 未处理异常 | 查看崩溃日志，导出后反馈 |

### 11.2 打包失败

检查：
1. Python已安装并添加到PATH
2. PyInstaller已安装：`pip install pyinstaller`
3. 源码文件完整
4. 磁盘空间充足

### 11.3 版本列表为空

检查：
1. `gromacs/` 目录是否存在
2. 目录下是否有 `bin/gmx.exe`
3. 版本目录名称是否以 `build-` 开头（会被过滤）

---

## 12. 附录

### 12.1 GROMACS命令速查

| 命令 | 功能 |
|------|------|
| `gmx grompp` | 预处理生成tpr文件 |
| `gmx mdrun` | 执行分子动力学模拟 |
| `gmx energy` | 提取能量数据 |
| `gmx rms` | RMSD分析 |
| `gmx rmsf` | RMSF分析 |
| `gmx sasa` | 溶剂可及表面积 |

### 12.2 GPU使用说明

确保NVIDIA驱动已安装：
```cmd
nvidia-smi
```

### 12.3 性能优化建议

1. 使用AVX-512版本（如果CPU支持）
2. 启用GPU加速
3. 合理设置线程数（建议为CPU核心数的1-2倍）
4. 使用PLUMED时注意性能开销

---

**文档版本**: v4.2.1  
**更新日期**: 2026-07-14  
**开发团队**: Trae