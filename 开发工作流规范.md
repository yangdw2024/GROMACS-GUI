# GROMACS GUI 开发工作流规范

> 本文档基于 v4.3.0 代码审查制定，目标是消除 AI 批量生成痕迹，建立人主导、AI 辅助的可持续开发流程。
> 所有后续开发必须遵循本规范；偏离规范时在提交信息中说明理由。

---

## 一、当前 AI 味问题清单

以下问题已在 v4.3.0 代码中确认，整改前不得新增功能：

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 1 | 主程序单文件 567KB / 约 13000 行 / 221 个方法，未拆分 | `source/gromacs_gui_v4.py` | 高 |
| 2 | 重复 import：`os`、`argparse` 各出现两次 | 主程序头部 | 中 |
| 3 | `core/` 实际 16 个模块，README 写 13 个，文档与代码不同步 | `source/core/`、README | 中 |
| 4 | `.gitignore` 和 `core/__init__.py` 中文注释乱码（GBK/UTF-8 混用） | 根目录、core | 高 |
| 5 | 测试脚本散落根目录（15 个），且被 `.gitignore` 忽略，不纳入版本控制 | 根目录 | 高 |
| 6 | git 仅 4 个提交，首次提交直接是完整版，无渐进开发历史 | `.git/` | 中 |
| 7 | 17 个编译脚本版本后缀混乱（v2/v3/final/final2），无归档 | `build_scripts/` | 低 |
| 8 | 根目录堆积 21 个临时报告/日志/崩溃文件，含 6.6MB 检测报告 | 根目录 | 中 |
| 9 | README 使用"NASA FDIR 级别""ISO 27001 级别"等夸张术语 | README.md | 中 |
| 10 | `source/` 下残留 11 个 `test_dir_*` 临时目录 | `source/` | 低 |
| 11 | 根目录残留 `cuda-keyring_1.1-1_all.deb.1` 重复下载文件 | 根目录 | 低 |
| 12 | 两个 Release zip 直接放在项目根目录（共 225MB） | 根目录 | 中 |

---

## 二、目录结构规范

### 2.1 目标结构

```
Trae_Gromacs/
├── source/
│   ├── main.py                     # 程序入口（<50行）
│   ├── gui/                        # GUI 层
│   │   ├── __init__.py
│   │   ├── main_window.py          # 主窗口 GromacsGUI
│   │   ├── dialogs/                # 对话框
│   │   │   ├── __init__.py
│   │   │   ├── error_diagnosis.py  # 错误诊断器+对话框
│   │   │   ├── mdp_editor.py       # MDP 可视化编辑器
│   │   │   ├── simulation_monitor.py # 实时监控
│   │   │   ├── custom_template.py  # 自定义模板
│   │   │   └── help_dialog.py      # 帮助
│   │   ├── widgets/                # 自定义控件
│   │   │   ├── __init__.py
│   │   │   └── curve_widget.py     # 实时曲线
│   │   └── workers/                # QThread 工作线程
│   │       ├── __init__.py
│   │       ├── gromacs_worker.py   # GROMACS 执行线程
│   │       └── data_extractor.py   # 数据提取线程
│   ├── core/                       # 核心服务层（精简后）
│   ├── simulation/                 # 模拟业务逻辑
│   │   ├── __init__.py
│   │   ├── mdp_builder.py          # MDP 文件生成
│   │   ├── workflow.py             # 工作流状态管理
│   │   └── analysis.py             # 结果分析命令封装
│   └── config/                     # 配置文件
├── tests/                          # 测试（纳入 git）
│   ├── __init__.py
│   ├── unit/
│   ├── integration/
│   └── conftest.py
├── build_scripts/                  # 构建脚本（只保留最终版）
├── resources/                      # 图标、教程
├── docs/                           # 文档
├── scripts/                        # 工具脚本
├── .gitignore
├── LICENSE
├── README.md
└── version.config
```

### 2.2 目录规则

- **根目录只放**：`.gitignore`、`LICENSE`、`README.md`、`version.config`、启动 `.bat`
- **临时文件**：测试输出、崩溃报告、检测报告一律放 `temp/`，且 `temp/` 不纳入 git
- **大文件**：GROMACS 引擎、miniconda3、Release zip 不进 git，已在 `.gitignore` 中忽略
- **禁止**在 `source/` 下创建 `test_dir_*` 临时目录，测试临时文件用 `tempfile.mkdtemp()`

---

## 三、代码组织规范

### 3.1 主程序拆分方案

当前 `gromacs_gui_v4.py` 的 11 个类按职责拆分：

| 当前类 | 行号 | 拆分目标 |
|--------|------|----------|
| `ErrorDiagnoser` | 472 | `gui/dialogs/error_diagnosis.py` |
| `ErrorDiagnosisDialog` | 806 | 同上 |
| `GromacsWorker` | 1183 | `gui/workers/gromacs_worker.py` |
| `CurveWidget` | 1438 | `gui/widgets/curve_widget.py` |
| `DataExtractorThread` | 1580 | `gui/workers/data_extractor.py` |
| `SimulationMonitorDialog` | 1752 | `gui/dialogs/simulation_monitor.py` |
| `HelpDialog` | 1987 | `gui/dialogs/help_dialog.py` |
| `CustomTemplateDialog` | 2254 | `gui/dialogs/custom_template.py` |
| `MdpEditorDialog` | 2470 | `gui/dialogs/mdp_editor.py` |
| `GromacsGUI` | 3651 | `gui/main_window.py`（继续按功能拆方法到 `simulation/`） |

### 3.2 拆分纪律

- **每批只搬移，不改逻辑**，搬完运行测试确认功能不变，再考虑优化
- 每批拆分对应一个 git commit，提交信息格式：`refactor: 拆分XXX到独立模块（零逻辑改动）`
- 拆分顺序：先拆独立的 Dialog/Worker/Widget（依赖少），最后拆主窗口
- 主窗口 `GromacsGUI` 约 9000 行，拆分 Dialog 后仍过大，需将业务逻辑（MDP 生成、命令构建、结果分析）抽到 `simulation/` 层

### 3.3 文件与函数规模

| 约束 | 限制 | 例外处理 |
|------|------|----------|
| 单文件行数 | ≤ 500 行 | 主窗口等特殊文件 ≤ 1000 行，超过必须继续拆 |
| 单函数行数 | ≤ 80 行 | 超过必须拆分为子函数 |
| 单函数参数 | ≤ 6 个 | 超过用 dataclass/dict 封装 |
| 嵌套深度 | ≤ 4 层 | 超过用提前 return 或子函数 |
| 重复代码 | 禁止复制粘贴超过 3 次 | 抽公共函数 |

### 3.4 core/ 模块精简

当前 16 个模块存在过度设计，按实际使用情况精简：

**保留（主程序实际 import 且有真实逻辑）**：
- `config_manager.py`、`logger.py`、`error_handler.py`
- `gromacs_service.py`、`version_manager.py`、`crash_handler.py`
- `resource_monitor.py`

**评估后决定（检查是否真的被调用，还是只初始化未使用）**：
- `workflow_engine.py`、`event_bus.py`、`review_mechanism.py`
- `correction_mechanism.py`、`audit_mechanism.py`、`auto_updater.py`

**处理方式**：
- 在主程序中搜索每个类名的实际调用次数
- 只初始化但从未调用方法的模块，删除
- 功能重叠的模块合并
- `wsl_gromacs_service.py`、`wsl_update_manager.py`：如不做 WSL 支持则删除

---

## 四、Git 工作流规范

### 4.1 分支策略

```
main          稳定分支，只接受 dev 合并，每次合并对应一个版本
  └── dev     开发分支，日常开发在此进行
       ├── feature/mdp-editor    功能分支
       ├── fix/gpu-detection     修复分支
       └── refactor/split-gui    重构分支
```

- 新功能从 `dev` 切 `feature/功能名` 分支
- bug 修复从 `dev` 切 `fix/问题名` 分支
- 重构从 `dev` 切 `refactor/内容` 分支
- 完成后合并回 `dev`，版本发布时 `dev` 合并到 `main` 并打 tag

### 4.2 提交规范

**提交信息格式**（中文描述）：

```
<type>: <简述>

<可选的详细说明>
```

type 取值：

| type | 用途 |
|------|------|
| `feat` | 新功能 |
| `fix` | bug 修复 |
| `refactor` | 重构（零逻辑改动） |
| `test` | 测试相关 |
| `docs` | 文档 |
| `build` | 构建/打包/依赖 |
| `chore` | 杂项（清理、配置） |

**示例**：
```
feat: MDP编辑器添加退火参数可视化标签页

fix: 修复切换GROMACS版本后实时监控仍使用旧路径的问题

refactor: 将GromacsWorker拆分到gui/workers/（零逻辑改动）

fix: 修复无NVIDIA GPU时GPU检测崩溃的问题
- 原因：nvidia-smi不存在时subprocess返回FileNotFoundError未捕获
- 修复：检测前先判断nvidia-smi是否存在，不存在直接返回CPU模式
```

### 4.3 提交粒度

- **每次提交只做一件事**：一个功能 / 一个 bug 修复 / 一批重构
- **单次提交不超过 500 行新增**（自动生成的文件除外）
- **禁止**一次性提交几千行"大爆炸"式代码
- **提交前必须能运行**：不提交半成品、不提交语法错误的代码
- 拆分重构时每批一个提交，提交信息标注"零逻辑改动"

### 4.4 .gitignore 修正

当前 `.gitignore` 存在两个问题：
1. 中文注释乱码——文件必须以 **UTF-8 无 BOM** 编码保存
2. `test_*.py` 被忽略——**测试代码必须纳入版本控制**，删除这条规则

修正后的测试相关规则：
```gitignore
# 临时测试脚本（不纳入git，正式测试放tests/）
/scratch_*.py
/temp/
```

---

## 五、AI 辅助编码规范

### 5.1 原则：人做决策，AI 做执行

| 工作 | 人 | AI |
|------|----|----|
| 决定做什么功能、为什么做 | ✅ | ❌ |
| 设计模块划分和接口 | ✅ | 可提建议 |
| 编写业务逻辑代码 | 审查 | ✅ 生成初稿 |
| GROMACS 编译、硬件兼容性调试 | ✅ | 提供方案 |
| 代码审查（查重复import、命名、规模） | ✅ | 辅助检查 |
| 写测试用例 | 确认场景 | ✅ 生成 |
| 写提交信息、changelog | 确认 | ✅ 起草 |
| 决定版本发布 | ✅ | ❌ |

### 5.2 AI 生成代码审查清单

每次 AI 生成代码后，提交前逐项检查：

- [ ] 无重复 import（用 `python -c "import ast; ..."` 或 IDE 检查）
- [ ] 无未使用的 import
- [ ] 无 `print("中文")` 调试输出残留（GUI 程序用 logger，控制台输出用英文）
- [ ] 无被注释掉的大段死代码
- [ ] 函数 ≤ 80 行，文件 ≤ 500 行
- [ ] 无 AI 套话注释（如"这个函数负责..."重复函数名）
- [ ] 异常处理不是空 `except: pass`
- [ ] 硬编码路径已提取为配置或常量
- [ ] 变量命名一致（不混用 `gmxPath`/`gmx_path`/`GMX_PATH`）
- [ ] 新增依赖已记录到 README 和 requirements

### 5.3 禁止的 AI 习惯

1. **禁止过度工程化**：不为"将来可能用到"写抽象层；3 个以下实现不需要工厂模式
2. **禁止夸张术语**：代码和文档中不出现"NASA FDIR""ISO 27001""企业级""军工级"等修辞
3. **禁止生成自检报告**：不产出 6.6MB 的"全面检测报告"；测试结果由 pytest 输出，需要留存写到 `docs/test-report-日期.md`，控制在 200 行内
4. **禁止版本后缀迭代**：不生成 `file_v2.py`、`file_final.py`、`file_final2.py`；直接修改原文件，靠 git 管理历史
5. **禁止单文件无限追加**：AI 生成新功能时，如果目标文件已超过 500 行，必须先拆分再添加

### 5.4 推荐的 AI 协作方式

**好的方式**：
- "把 `GromacsGUI` 里的 MDP 生成逻辑抽到 `simulation/mdp_builder.py`，不改逻辑"
- "给 `version_manager.py` 的 `_validate_version` 写 pytest 测试，覆盖 gmx.exe 不存在、CUDA DLL 缺失、PLUMED 文件为空三种情况"
- "这个报错是什么意思：[粘贴报错]，只分析原因，先别改代码"

**不好的方式**：
- "帮我做一个企业级的 GROMACS GUI，要有完善的架构"（过于宽泛，AI 会过度设计）
- "全面检测并修复所有问题"（AI 会生成大量无意义修改和报告）
- "优化一下代码"（不指定范围，AI 会大面积重写引入 bug）

---

## 六、测试规范

### 6.1 测试组织

```
tests/
├── conftest.py              # pytest 公共 fixture
├── unit/                    # 单元测试（不依赖 GROMACS 二进制）
│   ├── test_config_manager.py
│   ├── test_version_manager.py
│   ├── test_mdp_builder.py
│   └── test_logger.py
├── integration/             # 集成测试（需要 GROMACS，CI 可选跑）
│   ├── test_em_workflow.py
│   └── test_analysis.py
└── fixtures/                # 测试用样本文件
```

### 6.2 测试规则

- 测试框架统一用 **pytest**，不再写独立运行的 `test_xxx.py` 脚本
- 测试文件命名 `test_模块名.py`，测试函数命名 `test_场景_预期结果`
- **每个 bug 修复先写测试复现 bug，再修复**，测试函数名包含 bug 描述
- 单元测试不依赖 GROMACS 二进制（mock subprocess），集成测试才调用真实 gmx.exe
- 测试纳入 git，不再被 `.gitignore` 忽略
- 运行命令：`python -m pytest tests/ -v`

**示例**：
```python
# tests/unit/test_version_manager.py
def test_validate_version_missing_gmx_exe_returns_invalid(tmp_path):
    """gmx.exe 不存在时应标记为 INVALID"""
    vm = VersionManager()
    result = vm._validate_version(tmp_path)
    assert result.validity == VersionValidity.MISSING_BINARY
```

### 6.3 现有测试脚本处理

根目录的 15 个 `test_*.py` 不直接删除，按以下流程处理：
1. 逐个运行，确认哪些测试逻辑仍有价值
2. 有价值的改写为 pytest 格式移入 `tests/`
3. 一次性脚本（如 `test_sm120_binary.py`）移入 `scratch/` 存档
4. 重复的（`test_analysis_module_deep.py` 和 `_v2.py`）只保留 v2 的有效部分

---

## 七、文档与注释规范

### 7.1 文档体系

| 文件 | 用途 | 更新时机 |
|------|------|----------|
| `README.md` | 用户向：功能介绍、下载、快速开始 | 每次发版 |
| `docs/ARCHITECTURE.md` | 架构说明：模块划分、数据流 | 结构变更时 |
| `docs/CHANGELOG.md` | 版本变更记录 | 每次发版 |
| `开发工作流规范.md` | 本文档，开发规范 | 规范调整时 |
| `说明书.md` | 用户手册 | 功能变更时 |

### 7.2 README 写作要求

- **务实描述**：说"自动检测 GPU 并在无 GPU 时切换 CPU 模式"，不说"NASA FDIR 级纠错"
- **版本号必须与 `version.config` 同步**
- 模块数量必须与 `source/core/` 实际文件数一致
- 不堆砌设计模式名词（单例、观察者、策略模式等不在 README 出现，这些是实现细节）
- 中英文保持内容一致，不只是机翻

### 7.3 注释规范

- **注释用中文**，代码标识符用英文
- 注释解释"为什么"，不重复"做什么"
- 好注释：`# AMD Zen5 不支持 AVX512_VBMI，用基础指令替换避免 0xc000001d`
- 坏注释：`# 设置线程数为8`（代码本身已经说明了）
- 所有源文件统一 **UTF-8 无 BOM** 编码，禁止 GBK
- `print()` 输出信息用**英文**（Windows 控制台 GBK 环境下中文可能乱码）；GUI 界面显示用中文

### 7.4 CHANGELOG 格式

```markdown
## [4.4.0] - 2026-09-xx

### 新增
- MDP编辑器添加退火参数可视化标签页

### 修复
- 切换GROMACS版本后实时监控仍使用旧gmx.exe路径的问题
- 无NVIDIA GPU时GPU检测崩溃的问题

### 重构
- 将主程序按GUI对话框/工作线程/业务逻辑拆分为独立模块
```

---

## 八、版本发布规范

### 8.1 版本号规则

```
主版本.次版本.修订号
   4   .  3   .  0
```

- **主版本**：架构级重写或不兼容变更
- **次版本**：新功能，向下兼容
- **修订号**：bug 修复，无新功能

### 8.2 发布前检查清单

每次发版逐项确认：

- [ ] `version.config` 版本号已更新
- [ ] README 版本号已同步
- [ ] `docs/CHANGELOG.md` 已写变更记录
- [ ] `python -m pytest tests/ -v` 全部通过
- [ ] 主程序能正常启动（手动验证）
- [ ] 至少跑通一次完整工作流：EM → NVT → NPT → 生产
- [ ] 无 GPU 环境下能自动切换 CPU 模式
- [ ] PyInstaller 打包成功，exe 能在干净目录启动
- [ ] GROMACS 引擎版本与 `version_whitelist` 一致
- [ ] git 已打 tag：`git tag v4.4.0`

### 8.3 构建脚本整理

`build_scripts/` 只保留最终可用版本：

| 保留 | 删除/归档 |
|------|-----------|
| `build_release.bat`（最终打包脚本） | `build_gromacs_2026.3_avx512_cuda_sm120_v2.bat` |
| `build_release_v43.py`（如仍使用） | `build_gromacs_2026.3_avx512_cuda_sm120_v3.bat` |
| `installer_v43.iss`（Inno Setup 脚本） | `build_sm120_fixed.bat`、`build_sm120_final.bat` |
| 最终使用的 GROMACS 编译脚本 1-2 个 | 其他迭代过程脚本移入 `build_scripts/archive/` |

---

## 九、整改路线图

### 第一阶段：清理与止血（1 天）

- [ ] 修复 `.gitignore` 和 `core/__init__.py` 的中文乱码（统一 UTF-8）
- [ ] 从 `.gitignore` 删除 `test_*.py` 忽略规则
- [ ] 根目录临时文件归档：报告/日志/崩溃文件移入 `docs/history/` 或删除
- [ ] 删除根目录的两个 Release zip（已在 GitHub 发布，本地不保留）
- [ ] 删除 `cuda-keyring*.deb*`
- [ ] 清理 `source/test_dir_*` 11 个临时目录
- [ ] 删除重复 import
- [ ] 提交：`chore: 清理临时文件、修复编码、消除重复import`

### 第二阶段：测试归位（1-2 天）

- [ ] 创建 `tests/` 目录结构
- [ ] 逐个评估根目录 15 个测试脚本，有价值的改写为 pytest
- [ ] 为现有 core 模块补充基础单元测试
- [ ] 提交：`test: 建立pytest测试体系，迁移有效测试用例`

### 第三阶段：主程序拆分（3-5 天）

按以下顺序，每批一个 commit：

1. `refactor: 拆分CurveWidget到gui/widgets/（零逻辑改动）`
2. `refactor: 拆分GromacsWorker和DataExtractorThread到gui/workers/（零逻辑改动）`
3. `refactor: 拆分HelpDialog/CustomTemplateDialog到gui/dialogs/（零逻辑改动）`
4. `refactor: 拆分ErrorDiagnoser到gui/dialogs/（零逻辑改动）`
5. `refactor: 拆分SimulationMonitorDialog（零逻辑改动）`
6. `refactor: 拆分MdpEditorDialog（零逻辑改动）`
7. `refactor: 抽取MDP生成逻辑到simulation/mdp_builder.py（零逻辑改动）`
8. `refactor: 抽取结果分析逻辑到simulation/analysis.py（零逻辑改动）`
9. `refactor: 主窗口精简到1000行以内`

每批拆分后运行 `python -m pytest tests/ -v` 确认无回归。

### 第四阶段：core 精简（1-2 天）

- [ ] 统计每个 core 模块的实际调用次数
- [ ] 删除只初始化未使用的模块
- [ ] 合并功能重叠的模块
- [ ] 更新 README 模块数量
- [ ] 提交：`refactor: 精简core层，删除未使用的过度设计模块`

### 第五阶段：持续执行（长期）

- 后续所有开发按本规范执行
- 每次提交对照检查清单
- 每个 bug 修复先写测试
- 每个版本发布走发布检查清单

---

## 十、提交前自检清单

每次 `git commit` 前过一遍：

```
□ 这次提交只做了一件事
□ 提交信息符合 type: 描述 格式
□ 新增代码无重复 import、无未使用 import
□ 无 print("中文") 残留、无注释掉的死代码
□ 新增函数 ≤ 80 行，修改后文件 ≤ 500 行（主窗口 ≤ 1000 行）
□ 无 AI 套话注释和夸张术语
□ 异常已正确处理（无空 except: pass）
□ 测试已更新/新增，pytest 全部通过
□ 文档涉及的版本号/模块数已同步
□ 文件编码为 UTF-8 无 BOM
```

---

## 附：编码约定速查

```python
# 文件编码：UTF-8 无 BOM
# 注释语言：中文
# print 输出：英文（避免 Windows 控制台乱码）
# 类名：PascalCase        class GromacsWorker
# 方法/变量：snake_case    def select_best_version
# 常量：UPPER_CASE         DEFAULT_THREAD_COUNT = 8
# 私有方法：_前缀           def _validate_version
# Qt 信号：camelCase       data_ready = pyqtSignal(dict)

# 好的注释（解释为什么）：
# AMD Zen5 不支持 AVX512_VBMI，强制使用 AVX2 避免非法指令崩溃
if cpu_supports_avx512 and not cpu_supports_vbmi:
    simd = "AVX2"

# 坏的注释（重复代码）：
# 设置simd为AVX2
simd = "AVX2"
```

---

**文档版本**：v1.0
**制定日期**：2026-08-27
**适用版本**：GROMACS GUI v4.3.0 及后续所有版本
